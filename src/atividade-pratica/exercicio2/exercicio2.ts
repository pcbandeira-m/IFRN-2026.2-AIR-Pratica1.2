/*
2. CONCATENAÇÃO DE STRINGS
   Transforme o array concatenando as strings com 1 (um) espaço (" "). Utilize o método JOIN do Array, passando uma arrow function como parâmetro.
*/

let arrayFormatado = ((palavras: string[]) => palavras.join(" "));

// let palavras: string[] = ['Arrays', 'com', 'TypeScript'];
// console.log(arrayFormatado(palavras));

export default arrayFormatado;